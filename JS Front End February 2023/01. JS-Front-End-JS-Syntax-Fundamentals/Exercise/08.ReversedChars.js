function reversedChars(firstChar, secondChar, thirdChar) {
  let result = "";
  result = thirdChar + " " + secondChar + " " + firstChar;

  console.log(result);
}

reversedChars("A", "B", "C");
reversedChars("1", "L", "&");
